import {StyleSheet} from 'react-native';

const estilo = StyleSheet.create({
    tempo:{
        fontSize: 60, 
        textAlign: 'center',
        marginTop: 30, 
        marginBottom: 30
        , color: '#0b67cd' 
      }
})

export default estilo ;